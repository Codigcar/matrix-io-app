import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  currentTimeText: {
    color: '#FFF', 
    flex: 1,
  },
  durationText: {
    color: '#FFF', 
    flex: 1, 
    textAlign: 'right',
  },
});

export default styles;
