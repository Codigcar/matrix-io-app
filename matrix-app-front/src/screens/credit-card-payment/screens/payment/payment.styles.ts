import { rebrandingTheme } from 'matrix-ui-components';
import { StyleSheet } from 'react-native';

export const Styles = StyleSheet.create({
  input: {
    ...rebrandingTheme.textVariants.Subtitle20SemiBold,
    padding: 0,
  },
});
