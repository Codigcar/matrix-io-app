export type SlideProps = {
  title?: string;
  description?: string;
  partnerName?: string;
  image: string;
  id?: string;
  benefit: string;
  channel: 'Presencial' | 'Virtual' | 'Presencial y virtual';
};
