import { Box } from 'matrix-ui-components';
import React from 'react';

export const EmptyContainer = () => (
  <Box alignItems={'center'} backgroundColor={'black'} justifyContent={'center'} flex={1}   />
);

