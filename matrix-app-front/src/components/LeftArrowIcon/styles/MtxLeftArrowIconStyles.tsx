import EStyleSheet from 'react-native-extended-stylesheet';
import { ICON_SIZE } from 'matrix/src/utils/constants';

const styles = EStyleSheet.create({
    icon: {
        width: ICON_SIZE,
        height: ICON_SIZE,
    },
});

export default styles;
